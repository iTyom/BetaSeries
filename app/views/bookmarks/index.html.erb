
<%- @bookmarks.each do |bookmark| -%>
  <section class='sectionSeries'>
    <h2><%= link_to(bookmark.serie.name, series_path(bookmark.serie)) %></h2>
    <%= cl_image_tag bookmark.serie.photo, width: 199, height: 279,
                                    crop: :fill %>
  </section>
<% end %>
