<div class="collapse navbar-collapse">
  <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to("BetaSeries", root_path, class: "navbar-brand") %>
      <%= link_to("Mes favoris", bookmarks_path) %>

    </div>
  <ul class="nav navbar-nav navbar-right">
    <% if user_signed_in? %>
      <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button"
          aria-haspopup="true" aria-expanded="false">
            Bienvenue, <%= current_user.email %>
            <span class="caret"></span>
        </a>
        <ul class="dropdown-menu">
          <li>
            <%= link_to edit_user_registration_path do %>
              <i class="fa fa-user"></i> Modifier le profil
            <% end %>
          </li>
          <li>
            <%= link_to destroy_user_session_path, method: :delete do %>
              <i class="fa fa-sign-out"></i> Déconnexion
            <% end %>
          </li>
        </ul>
      </li>
    <% else %>
      <li><%= link_to("Connexion", new_user_session_path) %></li>
      <li><%= link_to("Inscription", new_user_registration_path) %></li>
    <% end %>
  </ul>
</div>
